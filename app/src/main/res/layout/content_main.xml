<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:showIn="@layout/activity_main"
    tools:context=".MainActivity"
    android:padding="16dp">
    <Button
        android:id="@+id/btnBackgroundChange"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:textSize="24sp"
        android:layout_marginBottom="16dp"
        android:text="@string/btnBackgroundChange"
        android:onClick="onColourButtonClicked"/>

    <Button
        android:id="@+id/btnButtonsChange"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_below="@id/btnBackgroundChange"
        android:textSize="24sp"
        android:layout_marginBottom="16dp"
        android:text="@string/btnButtonsChange"
        android:onClick="onColourButtonClicked"/>

    <Button
        android:id="@+id/btnFontChange"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_below="@id/btnButtonsChange"
        android:textSize="24sp"
        android:text="@string/btnFontChange"
        android:onClick="onColourButtonClicked"/>
</RelativeLayout>